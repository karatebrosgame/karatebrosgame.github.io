export interface NavItem {
  label: string;
  href: string;
}

export interface GameStats {
  rating: number;
  plays: string;
  category: string;
}

export interface FaqItem {
  question: string;
  answer: string;
}